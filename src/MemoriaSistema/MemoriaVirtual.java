package MemoriaSistema;

public class MemoriaVirtual {
    public MemoriaVirtual() {
        // Inicialización de la memoria virtual (simulada)
    }
    
    public void writePage(int pageNumber) {
        // Simula la escritura de la psagina al disco (SWAP)
    }
    
    public void readPage(int pageNumber) {
        // Simula la lectura de la pagina desde el disco (SWAP)
    }
}
